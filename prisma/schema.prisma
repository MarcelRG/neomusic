generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider     = "mysql"
    url          = env("DATABASE_URL")
    relationMode = "prisma"
}

model Genre {
    id       String @id @default(cuid())
    name     String @unique
    x        Int
    y        Int
    hex      String
    playlist String
}
